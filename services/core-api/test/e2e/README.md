# test/e2e/

End-to-end tests spanning multiple modules — TDD §5.

Distinct from the per-module `__tests__/unit/` and `__tests__/integration/` folders
(which test one module in isolation): tests here exercise real cross-module flows,
e.g. the "Revenue Spine" test (Lead → Contact + Deal → Pipeline → Won) called out as
the single most important test in the Task List's M4 milestone.

No tests exist yet.
