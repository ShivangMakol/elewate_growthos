{
  "name": "@elewate/core-api",
  "version": "0.0.0",
  "private": true,
  "description": "The modular monolith — Architecture Blueprint §5. Currently bootstrap-only: no authentication, no database logic, no business modules registered.",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "fastify": "^5.10.0",
    "graphql": "^16.14.2",
    "mercurius": "^16.10.0"
  },
  "devDependencies": {
    "@elewate/config": "workspace:*",
    "@types/node": "^22.20.1",
    "tsx": "^4.23.1",
    "typescript": "^5.7.2"
  }
}
