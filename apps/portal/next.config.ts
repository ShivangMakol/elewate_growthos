import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Intentionally minimal for the app-shell milestone — no business logic,
  // no custom routing/rewrites/headers yet. Extended as real modules land.
};

export default nextConfig;
